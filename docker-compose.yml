version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./backend/coffee_history.db:/app/backend/coffee_history.db
    restart: always
